# Environment (development, staging, production)
ENVIRONMENT=development

# CORS allowed origins (JSON list format)
ALLOWED_ORIGINS='["http://localhost:3000", "http://localhost:8080", "http://localhost:8000"]'

# API Key for production authentication
API_KEY=

# Rate limiting settings
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_PERIOD=60

# Input validation limits
MAX_MESSAGES=1000
MAX_MESSAGE_LENGTH=5000
MAX_TOTAL_CHARACTERS=100000

# Server settings
SERVER_HOST=0.0.0.0
SERVER_PORT=8000
SERVER_RELOAD=true

# Dispatcher settings
DISPATCHER_MAX_WORKERS=5
DISPATCHER_MAX_QUEUE_SIZE=100
DISPATCHER_TASK_TIMEOUT=60.0
DISPATCHER_SHUTDOWN_TIMEOUT=30.0
